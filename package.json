{
  "name": "extension-labelstudio",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome extension that auto-fills the Label Studio classification form from a pre-classified document spreadsheet.",
  "scripts": {
    "build:data": "node scripts/xlsx-to-json.js Relevancia_Documentos.xlsx data/relevancia.json"
  },
  "devDependencies": {
    "xlsx": "^0.18.5"
  }
}
